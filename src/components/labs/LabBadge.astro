---
// src/components/labs/LabBadge.astro
// A simple badge component for displaying lab information in a compact way
const { lab } = Astro.props;

const displayName = lab.shortname || lab.short_name || lab.name || `Lab #${lab.id || '?'}`;
const slug = lab.slug || `lab-${lab.id}`;
---

<a href={`/labs/${slug}`} class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-green-100 text-green-800 hover:bg-green-200">
  <i class="fas fa-chevron-right"></i>
  {displayName}
</a>